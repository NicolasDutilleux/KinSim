// Sequel II 48-plex — raw subreads, PacBio-provided assemblies, SMRT 25.3 ipdSummary
// (subreads have native ip/pw tags, no bystrandify needed).
params {
    platform    = 'subread'
    assemble    = false
    ipd_backend = 'container'
}
